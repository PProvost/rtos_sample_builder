{
    "dockerFile": "Dockerfile",

    "extensions": [
        "xaver.clang-format",
        "zixuanwang.linkerscript",
        "twxs.cmake",
        "ms-vscode.cmake-tools"
    ],

    "remoteUser": "vscode",

    "postCreateCommand": "git submodule update --init && ./rebuild.sh"
}